# Claude API key (required, server-side only)
ANTHROPIC_API_KEY=

# App PIN hash (SHA-256 hex of the chosen PIN)
# Generate: echo -n "yourpin" | sha256sum
APP_PIN_HASH=

# Session secret for signing auth tokens
SESSION_SECRET=
