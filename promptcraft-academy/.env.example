# ==========================================
# PROMPTCRAFT ACADEMY — Environment Variables
# Copy to .env and fill in your keys
# ==========================================

# Backend API
EXPO_PUBLIC_API_URL=http://localhost:3001/api

# AI Providers — add at least one
# Groq (llama3-8b-8192 — very fast, cheapest) — PRIMARY
EXPO_PUBLIC_GROQ_API_KEY=

# Claude (claude-haiku-4-5 — higher quality) — FALLBACK
EXPO_PUBLIC_CLAUDE_API_KEY=

# AI routing strategy: primary | fallback | cheapest | fastest
# fallback = try Groq first, fall back to Claude if it fails
EXPO_PUBLIC_AI_STRATEGY=fallback
